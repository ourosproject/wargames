interface:
  display_name: "Wargames"
  short_description: "Repo-specific patterns and workflows for wargames"
  default_prompt: "Use the wargames repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true