# secrets & lab-specific config — NEVER commit these
range.config.json
.env
*.pem
*.key
id_rsa*
id_ed25519*
known_hosts

# dependencies / build / heavy artifacts
target/
node_modules/
dist/
downloads/
*.iso
*.qcow2
*.tar.gz
*.tar.zst

# os / editor noise
.DS_Store
*.log

# local tooling config
.claude/
